{
  "version": 2,
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/web/$1"
    }
  ],
  "buildCommand": "rm -rf node_modules apps/web/node_modules packages/*/node_modules && bun install && cd apps/web && bun run build",
  "installCommand": "bun install --no-cache --force",
  "ignoreCommand": "echo 'Force complete rebuild for Drizzle fix'",
  "functions": {
    "apps/web/src/app/api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}